/**
 * Locally hosted images (served from /public/img) so the site works on any host
 * without depending on an external asset CDN.
 */
export const logoAsset = { url: "/img/logo.webp" };
export const heroAsset = { url: "/img/7665bef6.webp" };
